<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="soannpcn_apppassword.Views.LoginPage"
             Shell.NavBarIsVisible="False">
    <ContentPage.Content>

        

        <Grid BackgroundColor="#33393E">

            <Frame Margin="40" CornerRadius="50">
                <StackLayout Padding="10,0,10,0" VerticalOptions="Center">
                    <Image Source="Resources/drawable/logo_s.png"/>
                    <Label Text="Your password is key"/>
                    <Entry x:Name="TB_User" Text="{Binding User, Mode=TwoWay}" Placeholder="User"/>
                    <Entry x:Name="TB_Password" Text="{Binding Password, Mode=TwoWay}" IsPassword="True" Placeholder="Password"/>
                    <Button VerticalOptions="Center" Text="Login" Command="{Binding LoginCommand}" BackgroundColor="#33393E" TextColor="White" CornerRadius="10" HeightRequest="35"/>

                </StackLayout>
            </Frame>
    
        </Grid>
        
    </ContentPage.Content>
    
</ContentPage>